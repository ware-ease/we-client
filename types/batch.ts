/* eslint-disable @typescript-eslint/no-explicit-any */
export type Batch = {
  id: string;
  productId: string;
  supplierId?: string;
  code: string;
  name: string;
  mfgDate: string;
  expDate: string;
};
